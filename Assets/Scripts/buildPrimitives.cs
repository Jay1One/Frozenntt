using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class buildPrimitives : MonoBehaviour
{
    // Start is called before the first frame update
    private void Start()
    {
        //GameObject piplane = GameObject.CreatePrimitive(PrimitiveType.Plane);
        //GameObject sphere = GameObject.CreatePrimitive(PrimitiveType.Sphere);
        //GameObject cube = GameObject.CreatePrimitive(PrimitiveType.Cube);
        //GameObject capsule = GameObject.CreatePrimitive(PrimitiveType.Capsule);
        //capsule.transform.localEulerAngles = new Vector3(90f, 0f, 0f);
        //cube.transform.position = new Vector3(0f, 0.2f, 1f);
        //sphere.transform.position = new Vector3(4f, 5f, 2f);
        //sphere.transform.localScale = new Vector3(2f, 3f, 2f);


    }

 
}
